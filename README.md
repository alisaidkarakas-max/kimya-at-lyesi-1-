# kimya-at-lyesi-1-
kimya atölyesi 1 
gazlar.html
Gazlar Ultimate Pro body { font-family: Arial; margin:0; background:#eef2f7; transition:0.3s; } .dark { background:#121212; color:white; } header { background:linear-gradient(45deg,#0d6efd,#00c6ff); color:white; padding:25px; text-align:center; } nav { background:#222; padding:12px; text-align:center; } nav a { color:white; margin:15px; text-decoration:none; font-weight:bold; } section { padding:20px; } .card { background:white; padding:20px; margin:15px 0; border-radius:15px; box-shadow:0 4px 10px rgba(0,0,0,0.1);} .dark .card { background:#1e1e1e; } button { padding:10px 15px; background:#0d6efd; color:white; border:none; border-radius:8px; cursor:pointer; margin:5px; } .correct { color:lime; } .wrong { color:red; } let score = 0; let current = 0; let time = 60; const questions = [ {q:"Basınç artarsa hacim ne olur?", options:["Artar","Azalır","Değişmez","Yok olur"], a:"Azalır"}, {q:"0°C kaç Kelvin?", options:["100","273","300","0"], a:"273"}, {q:"PV = nRT nedir?", options:["Boyle","İdeal Gaz","Charles","Avogadro"], a:"İdeal Gaz"}, {q:"11.2 L gaz kaç mol?", options:["1","0.5","2","3"], a:"0.5"} ]; function loadQuestion(){ let q = questions[current]; document.getElementById("question").innerText = q.q; let opts = ""; q.options.forEach(opt => { opts += <button onclick="checkAnswer('${opt}')">${opt}</button>; }); document.getElementById("options").innerHTML = opts; document.getElementById("feedback").innerText = ""; } function checkAnswer(ans){ let correct = questions[current].a; let fb = document.getElementById("feedback"); if(ans == correct){ score++; fb.innerHTML="✔ Doğru"; fb.className="correct"; } else { fb.innerHTML="✘ Yanlış ("+correct+")"; fb.className="wrong"; } } function nextQuestion(){ current++; if(current < questions.length){ loadQuestion(); } else { document.getElementById("quiz").innerHTML = <h2>Sınav Bitti</h2><p>Puan: ${score}/${questions.length}</p>; } } function toggleDark(){ document.body.classList.toggle("dark"); } function startTimer(){ setInterval(()=>{ if(time>0){ time--; document.getElementById("timer").innerText = "Süre: "+time; } },1000); } window.onload = function(){ loadQuestion(); startTimer(); }
